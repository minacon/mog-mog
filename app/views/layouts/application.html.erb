<!DOCTYPE html>
<html>
  <head>
    <title>MogMog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <nav>
        <ul class=snsicon>
          <i class="fab fa-instagram"></i>
          <i class="fab fa-twitter"></i>
          <i class="fab fa-facebook"></i>
        </ul>
      </nav>
      <div class=h-main>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          <a class="post" href="/tweets/new">投稿する</a>
        </div>
        <% else %>
          <div class="user">
            <%= link_to "ログイン", new_user_session_path, class: 'post' %>
            <%= link_to "新規登録", new_user_registration_path, class: 'post' %>
          </div>
        <% end %>
      </div>
      <div class=log-wrapper>
        <div class=main-log>MOG MOG</div>
        <p class=sub-log>食べるは、しあわせ。</p>
      </div>
      <div class=seach-wrapper>
        <i class="fas fa-search"></i>
        <%= form_tag('/tweets/search', method: :get) do %>
          <input class="search-box" name="keyword" placeholder="  キーワードで検索 " type="text" value=""></input>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <div class=footer-box>Are You Happy?</div>
    </footer>
  </body>
</html>
